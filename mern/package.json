{
  "name": "my-node-server",
  "version": "1.0.0",
  "scripts": {
    // This "start" script tells Node how to run your server
    "start": "node server.js"
  },
  "dependencies": {
    // Server dependencies
    "express": "^4.18.2",
    "mongoose": "^6.8.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "multer": "^1.4.5-lts.1",
    
    // If you actually need framer-motion on the server side
    // (uncommon; it's usually for client-side React)
    "framer-motion": "^12.4.7"
  }
}
